CREATE OR REPLACE VIEW TEST_SCHEMA.VW_CUSTOMER_ORDERS AS
SELECT
    c.CUSTOMER_ID,
    c.CUSTOMER_NAME,
    o.ORDER_ID,
    o.AMOUNT
FROM TEST_SCHEMA.CUSTOMERS c
JOIN TEST_SCHEMA.ORDERS o
    ON c.CUSTOMER_ID = o.CUSTOMER_ID;
