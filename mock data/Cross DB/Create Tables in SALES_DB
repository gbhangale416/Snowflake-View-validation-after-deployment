CREATE OR REPLACE SCHEMA SALES_DB.PUBLIC;

CREATE OR REPLACE TABLE SALES_DB.PUBLIC.CUSTOMERS (
    CUSTOMER_ID INT,
    CUSTOMER_NAME STRING,
    CITY STRING
);

CREATE OR REPLACE TABLE SALES_DB.PUBLIC.ORDERS (
    ORDER_ID INT,
    CUSTOMER_ID INT,
    AMOUNT NUMBER(10,2)
);

INSERT INTO SALES_DB.PUBLIC.CUSTOMERS VALUES
(1, 'John', 'New York'),
(2, 'Alice', 'London');

INSERT INTO SALES_DB.PUBLIC.ORDERS VALUES
(1001, 1, 500),
(1002, 2, 750);
